{% include "core/header.twig" %}

<body id="page-top">

{% include "core/main-nav.twig" %}

<section class="bg-primary" id="about">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 text-center">
                <h2 class="section-heading">{{ episode.getTitle() }}</h2>
                <hr class="light">
                <p>{{ episode.getPublishDate() }}</p>
                <p>{{ episode.getContent()|raw }}</p>
                <a href="#" id="subscribe-button" class="btn btn-default btn-xl">Subscribe!</a>
            </div>
        </div>
    </div>
</section>

{% include "core/contact.twig" %}

{% include "core/footer.twig" %}
